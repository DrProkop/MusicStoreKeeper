<UserControl x:Class="MusicStoreKeeper.Vmv.View.ArtistPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderBrush="CadetBlue" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- artist name -->
                <TextBlock Grid.Row="0" Text="{Binding ItemName}"/>
                <!-- artist image -->
                <Image Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Source="{Binding ArtistImage}" Margin="10" Stretch="None" />
                <!-- artist profile -->
                <TextBlock Grid.Row="2" Text="{Binding Profile}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>